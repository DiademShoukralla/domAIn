server {
  listen 80;
  listen [::]:80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /app/ {
    try_files $uri $uri/ /app/index.html;
  }

  location = /app {
    return 301 /app/;
  }
}
